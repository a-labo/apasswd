/**
 * Test case for fromSpecString.
 * Runs with mocha.
 */
'use strict'

const fromSpecString = require('../lib/from_spec_string.js')
const assert = require('assert')
const co = require('co')

describe('from-spec-string', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('From spec string', () => co(function * () {

  }))
})

/* global describe, before, after, it */
