/**
 * Test case for newSalt.
 * Runs with mocha.
 */
'use strict'

const newSalt = require('../lib/salting/new_salt.js')
const assert = require('assert')
const co = require('co')

describe('new-salt', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('New salt', () => co(function * () {

  }))
})

/* global describe, before, after, it */
